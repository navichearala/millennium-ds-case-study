# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.ipynb_checkpoints/

# Environment — never commit API keys
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Per-candidate JSON is regenerated by the pipeline on every run
data/parsed/

# Private interview preparation notes are deliberately kept out of this repo:
# if the repository is ever shared with the hiring team, a file of anticipated
# questions and rehearsed answers is not something to hand over.
INTERVIEW_NOTES.md
